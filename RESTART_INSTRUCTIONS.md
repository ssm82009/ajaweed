# 🔄 تعليمات إعادة تشغيل الخادم

## المشكلة:
بعد تحديث ملفات الاتصال بقاعدة البيانات، يجب إعادة تشغيل الخادم لتطبيق التغييرات.

---

## الحل:

### 1️⃣ إذا كنت تشغل الخادم محلياً:

```bash
# أوقف الخادم الحالي (اضغط Ctrl+C في terminal)

# ثم أعد تشغيله:
npm run dev:server
```

---

### 2️⃣ إذا كان على cPanel (السيرفر الفعلي):

#### الطريقة الأولى: من cPanel Dashboard
1. اذهب إلى **Setup Node.js App**
2. ابحث عن التطبيق
3. اضغط زر **Restart**

#### الطريقة الثانية: من SSH
```bash
ssh ajaweed@104.207.66.134
cd /home/ajaweed/app
pm2 restart all
# أو
pm2 restart 0
```

---

## ✅ التحقق من نجاح الاتصال:

بعد إعادة التشغيل، افتح المتصفح واذهب إلى:
```
http://localhost:3000/api/health
```

أو على السيرفر:
```
https://ajaweed.online/api/health
```

يجب أن يظهر:
```json
{
  "status": "ok",
  "time": "2025-02-08T..."
}
```

---

## 🔧 التحقق من وجود ملف .env على السيرفر:

### من cPanel File Manager:
1. اذهب إلى **File Manager**
2. افتح `/home/ajaweed/app/`
3. تأكد أن ملف `.env` موجود

### إذا غير موجود، أنشئه:

**محتوى ملف `.env`:**
```
TURSO_URL=libsql://ajaweed-ssm82009.aws-eu-west-1.turso.io
TURSO_AUTH_TOKEN=eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJhIjoicnciLCJpYXQiOjE3NzA1NDQ3ODIsImlkIjoiMzE3NmIxY2YtYjk1MS00ODVhLWEwMDctMWMxYjg5NTU0N2ZiIiwicmlkIjoiMjNiYmY3NDEtZTE2OS00NTUyLWFlY2EtOGM3MDA2MTVhODU5In0.QMvPwyBHsacAdqf3ZLg-q27FwoHIwQLUhIEM_pCtH01THY3ehw1m0rO_Ojv8-HzqjtG8I5VWi0UJ53-ahXICDQ
```

---

## 📋 ماذا تم تحديثه؟

| الملف | التغيير |
|-------|---------|
| `server/db.mjs` | إضافة تسجيل معلومات الاتصال |
| `server/index.mjs` | استيراد دالة اختبار الاتصال |
| `.env` | تحديث رمز المصادقة |

---

## 🎯 بعد إعادة التشغيل:

سيعمل جميع الأنظمة:
- ✅ نظام الملاحظات
- ✅ نظام الغياب
- ✅ نظام المتفوقين
- ✅ نظام الخروج
- ✅ نظام الزوار

---

## ❓ إذا استمرت المشكلة:

1. تحقق من logs الخادم:
   ```bash
   pm2 logs
   ```

2. شغّل سكريبت الاختبار:
   ```bash
   node debug-db.mjs
   ```

3. أخبرني بالرسالة التي تظهر في console
